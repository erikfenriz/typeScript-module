/*
This function used for simple logging
 */
const logging = (name: string): void => {
  // use console.log to display the message
  console.log(`Hello, ${name}!`);
};

// invoke the function to greet a user
logging("World");
